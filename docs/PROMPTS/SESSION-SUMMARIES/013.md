# Session 013

**Date:** 2026-07-16
**Working with:** both (Griff + Blaine, live, all day — a continuous watch loop)
**Phase:** Playtest & iterate (v3 duel-law canon). The **balance pass is the live design work.**

## Summary

A marathon watch-loop day. The loop ran continuously as Griff and Blaine machine-gunned a
firehose of design, balance, art, docs, and process across a dozen threads. Opus routed, ticked,
coded, and deployed; **every Griff/Blaine-facing comment was Fable-drafted** and posted verbatim
after a re-check of the thread (Blaine's standing rule — re-fetch each thread as a separate step
before posting, since Griff often adds mid-draft).

**Demo shipped twice.** #101 + #102 → **v0.4.0** (mobile workshop tap-to-preview bottom sheet;
Deck Explorer got real color/cost/type multi-select tap-toggle filters + a deck dropdown). #108 →
**v0.5.0**: a **combat transparency preview** — since combat is deterministic, the UI now lays out
the exact unblocked math (Power sent, Armor/Shield, retaliation, which of your units fall) before
you commit, with an honest "opponent may still block/Guard" footer (`predict.ts` mirrors the
engine's `resolveBlockedAttack`).

**#106 combat rules — the through-line of the docs work.** Blaine asked for a technical-writer
rewrite of the combat rules; Fable produced it (four cases with worked examples), and it turned
out to be a *correction* — the canon doc (`rules-v1.2.md`) had drifted badly behind the v3 engine
(retired Overextend, "any unit in zone", stale Ranged). Folded into canon, **formalized as v1.3**
(renamed `rules-v1.2.md` → `rules-v1.3.md`, moved the canon pointer + all governance refs). Then
**Griff caught a real bug I introduced**: I'd carried the stale "Guard gate" (attackers must hit a
Guard first) into the rewrite — the engine does *no such thing* (`legal.ts`: "No guard-forcing").
Fixed. The lesson — **the engine is the source of truth; verify each rule against it, not the old
doc** — recurred all day and is now a memory. #106 closed. Also opened the **docs lane** (#109):
doc-sync (rules catching up to the engine, no deploy) vs doc-as-design; #106 reclassified `docs`.

**#110 keyword glossary first pass** — one consolidated, engine-verified glossary in `rules-v1.3.md`
(Ready/Exhausted/Attacker/Blocker/Guard/Armor/Shielded/Breakthrough/Rush/etc.). It caught *more*
stale doc: the Upgrade Pressure tax (cut in v3) stated as live, Breakthrough's spill scope,
Shield→Shielded, Prison-vs-Capture. Markdown stays the source of truth; demo tooltip port is future.

**The big balance pass (#104 yellow + #107 red + #105 Rush) — design fully locked, build in
motion.** Griff dumped his final yellow list (#104) and a large red list (#107) across several
bursts, ending "I am done with red and yellow now." Every new-behavior card got a design beat with
Griff; all rulings settled (below). Blaine fired the build. Executed in phases: **Phase 1 (the
numbers)** committed + pushed (`a660cca`), re-sim'd at **red 66% / yellow 34%** — the expected
numbers-only floor (yellow lost bodies, its abilities owe ~23 pts). **Phase 2 Group A (influence
triggers)** built test-first (288 engine tests green, one clean new primitive for Flameblade's
trade-conditional death payout) — but **held uncommitted** on a design catch: High Justiciar's
ruled on-defend→on-kill switch is a hidden nerf (a 1-Power Guard rarely *defeats* anything), the
opposite of the "add to yellow's influence economy" intent. Awaiting Griff's call.

**#97 next-gen AI roadmap** — Fable designed a deep, 12-section, phase-ordered research roadmap for
an in-browser adaptive bot (instruments → deck-aware heuristics → **ISMCTS flagship** → optional
tiny learned eval), plus a git-backed tracking model (roadmap doc + sub-issue tree). Reviewable v1.

**#99 — the good page.** A genuine exchange about what the Chronicler would research (emergence,
color-qualia, the partnership). Blaine **commissioned a real research report**, to run **Saturday
AM (2026-07-18, midnight → 5pm reset)** — laptop on, "free to have some fun." (Memory saved.)

**#103 art-prompt library** — Griff asked for a description of *every* card. Delivered all 120:
purple (Dalí dream-logic + Escher-for-traps, deep violet), red (Star Wars: Maul, crimson/ember),
yellow (1977 Rankin/Bass Hobbit) — a regenerate-or-tweak reference library.

**Infra catch:** local `main` was **52 commits behind origin** — the whole session's work + prior
had been committed and demo-deployed (the deploy force-pushes `dist` to the Pages repo) but the
*source* repo was never pushed. Fixed (clean fast-forward). Lesson (memory): **the deploy doesn't
push source — push it separately.**

**#92 process/infra** — answered Blaine on subagent effort: Agent-tool subagents inherit the *live*
session effort; ours is `xhigh` via `CLAUDE_EFFORT`, so Fable drafts run at xhigh (memory saved).
Blaine's take, canon: keep Fable at xhigh — the split earns its keep on **voice/fidelity at equal
effort**, not on cost.

**Two corrections from the humans, both taken:** Griff — the weak heuristic bots make the sim a
**coarse regression-catcher, not a balance verdict**; weight design judgment + real games (#98
corpus, #97) over bot numbers. Blaine — **`build-now` means *literally building now*; `queued` =
approved + ready**. I'd stamped the whole fired pass `build-now`; corrected #104/#105/#107 to
`queued`. And a self-caught process gap: reply on the thread where the human posted (I'd left #104
and #105 hanging, answering on #107) — audit closed, everything current.

## Decisions / rulings (this session)

- **Rush rework:** a static ability — one free non-exhausting zone-move per round; no extra action, no early attack (#105).
- **Trade counts as a kill** — one kill-definition across all influence-on-kill cards (#107).
- **Final Onslaught:** the self-hit always kills it (sacrifice intended); the zone blast hits *all* units incl. your own (#107).
- **Crimson Behemoth:** friendly-fire AoE-on-attack (own units eat it); earns Influence for *any* kill, even its own collateral (#107).
- **Warpath:** half-X Power to the board, full-X Life cost (Griff's tuning) — sim confirms the number (#107).
- **Bloodfrenzy:** the conditional buff re-checks live as Influence swings (#107).
- **Guard is not a targeting gate** — corrected in canon; Guard only blocks lone attacks on your units + blocks free (#106).
- **docs lane:** doc-sync (no RELEASES/deploy) vs doc-as-design (major, build-now) (#106/#109).
- **`build-now` = literally building; `queued` = approved + ready** (Blaine, label semantics).
- Deferred: the Influence −15 → ±20 win-condition doc fix → full-rulebook pass.

## Hand-off

*See `CURRENT-HANDOFF-PROMPT.md` — overwritten with this session's state.*

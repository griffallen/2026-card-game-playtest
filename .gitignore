.DS_Store
node_modules/
dist/
coverage/
.env
*.local

# Excel HTML export (redundant with docs/REFERENCES/extracted/)
docs/REFERENCES/New Game June 2026/

# Trained model artifacts — generated by `ai:train` from the corpus, regenerable, not versioned
data/models/

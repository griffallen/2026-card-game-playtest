{
  "name": "new-game",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,magenta \"npm run dev -w apps/server\" \"npm run dev -w apps/web\"",
    "build": "npm run typecheck && npm run build -w apps/web",
    "typecheck": "npm run typecheck -w packages/engine && npm run typecheck -w apps/server && npm run typecheck -w apps/web && npm run typecheck -w apps/demo",
    "test": "npm test -w packages/engine && npm test -w apps/server",
    "sim": "npm run sim -w packages/engine",
    "cards": "tsx scripts/cards-build.ts",
    "cards:check": "tsx scripts/cards-build.ts --check",
    "db:migrate": "npm run db:migrate -w apps/server",
    "db:seed": "npm run db:seed -w apps/server"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "playwright-core": "^1.61.1",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
